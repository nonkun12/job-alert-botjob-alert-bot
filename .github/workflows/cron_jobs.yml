name: Fetch Jobs and Notify Slack

on:
  schedule:
    # 毎日日本時間の朝9:00（UTCでは夜0:00）に実行
    - cron: '0 0 * * *'
  # 手動でテスト実行できるようにする設定
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
    # 1. コードのチェックアウト（最新のv4またはリリースされていればv5等に更新）
    - name: Checkout repository
      uses: actions/checkout@v4

    # 2. Python環境のセットアップ
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11' # '3.x' ではなく具体的なマイナーバージョンを指定するとより安定します

    # 3. 依存ライブラリのインストール
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    # 4. Pythonスクリプトの実行（環境変数を渡す）
    - name: Run Fetch Script
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      run: python fetch_jobs.py
